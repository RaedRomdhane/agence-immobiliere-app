# ==========================================
# ENVIRONNEMENT STAGING
# ==========================================
# Ce fichier contient les variables d'environnement
# pour l'environnement de staging/pré-production
# ==========================================

# Environnement
NODE_ENV=staging

# Serveur
PORT=5000

# Base de données MongoDB
# Format: mongodb://[username:password@]host[:port]/database[?options]
# Pour MongoDB Atlas (staging): mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER>.mongodb.net/<DATABASE>
# Pour MongoDB local: mongodb://localhost:27017/database
MONGODB_URI=mongodb://localhost:27017/agence-immobiliere-staging

# JWT (JSON Web Tokens)
# ⚠️ IMPORTANT: Générer une clé secrète forte en production
# Commande: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=staging_secret_key_change_me_in_real_staging
JWT_EXPIRE=7d

# OAuth Google (optionnel)
# Obtenir les credentials: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_staging_google_client_id
GOOGLE_CLIENT_SECRET=your_staging_google_client_secret

# Email / SMTP (optionnel - pour plus tard)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<YOUR_EMAIL>@agence-immobiliere.fr
SMTP_PASS=<YOUR_APP_PASSWORD>

# URLs
API_URL=http://staging-api.agence-immobiliere.fr
CLIENT_URL=http://staging.agence-immobiliere.fr

# Logging
LOG_LEVEL=debug

# Rate Limiting (requêtes par minute)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Session
SESSION_SECRET=staging_session_secret_change_me

# CORS (origines autorisées, séparées par des virgules)
CORS_ORIGIN=http://staging.agence-immobiliere.fr,http://localhost:3000

# Uploads
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# ==========================================
# NOTES DE SÉCURITÉ POUR LE STAGING RÉEL
# ==========================================
# 1. Utiliser MongoDB Atlas ou un serveur MongoDB dédié
# 2. Activer l'authentification MongoDB
# 3. Générer des secrets forts et uniques
# 4. Utiliser HTTPS en staging
# 5. Configurer un pare-feu
# 6. Activer les logs détaillés
# 7. Limiter les accès par IP si possible
# 8. Ne JAMAIS committer ce fichier avec de vraies credentials
# ==========================================
